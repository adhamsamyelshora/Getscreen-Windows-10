name: GetScreen Windows (8H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=adhamsamyelshora@gmail.com" > secrets.txt
         Invoke-WebRequest -Uri "" -OutFile "down.bat"
         cmd /c down.bat

      - name: Time Counter
        run: python time.py
