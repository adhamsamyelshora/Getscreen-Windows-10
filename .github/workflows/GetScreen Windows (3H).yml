name: GetScreen Windows (3H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=Your GetScreen Mail" > secrets.txt
         Invoke-WebRequest -Uri "" -OutFile "down.bat"
         cmd /c down.bat

      - name: Time Counter
        run: python time.py
